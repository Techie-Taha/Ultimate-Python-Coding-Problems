# write a program to detect double space in a string
fact = "Python  is  a  easy  language"
print(fact.find("ge")) # we created some double spaces and for each single space, 1 index is available. So in the terminal we will see 27, which is ge, like only g